import "./cartMenu.scss";
export const CartMenu = ({ item, onClick }) => {
  console.log(item);
  return (
    <div className="cartMenu">
      {/* <div className="cartMenuGameList">{item.map((game) => game.title)}</div> */}
    </div>
  );
};
